<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_load_accounts"
        android:title="@string/action_load_accounts"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_save_csv"
        android:title="@string/action_save_csv"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_delete_csv"
        android:title="@string/action_delete_csv"
        app:showAsAction="ifRoom"/>

</menu>